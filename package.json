{
  "name": "brio-software",
  "version": "1.0.0",
  "private": true,
  "description": "Brio · gestionale fast bar (cassa, magazzino, fornitori, dashboard, kiosk, KDS)",
  "scripts": {
    "build": "node build.js",
    "vercel-build": "echo 'No build needed: app.js already minified by GitHub Action'"
  },
  "devDependencies": {
    "terser": "^5.47.1"
  }
}
